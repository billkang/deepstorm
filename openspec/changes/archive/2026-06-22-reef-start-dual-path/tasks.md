## 1. 修改入口路由

- [x] 1.1 在 SKILL.md.tmpl 的 `## 功能概述` 和 `## 前置条件` 之间，新增 `## 入口路由` 章节，包含入口决策逻辑的描述
- [x] 1.2 在入口路由章节中添加 Mermaid 流程图，展示 Path A / Path B 的分支路径
- [x] 1.3 更新概述流程图（`## 功能概述` 中的 mermaid），展示两条路径在 superpowers 门禁处汇合

## 2. Path A（Issue 驱动）结构调整

- [x] 2.1 当前阶段一（获取需求）保持不变，在其标题后标注"Path A only"
- [x] 2.2 当前阶段二（创建分支）标注"Path A"——Path A 在此处创建分支
- [x] 2.3 当前阶段三（SDD 文档生成）保持不变——两条路径共享，标注为"两条路径共享"
- [x] 2.4 superpowers 门节保持不变——两条路径共享
- [x] 2.5 当前阶段四（TDD 实现）标注"两条路径共享"——但 Path B 需要在此处先创建分支
- [x] 2.6 当前阶段五（分支结束处理）保持不变

## 3. 新增 Path B（开放讨论流程）

- [x] 3.1 在工作流章节下新增 Path B 的阶段定义：阶段一的 BP（讨论阶段）
- [x] 3.2 编写 Path B 阶段一流程：创建 OpenSpec change → 结构化需求讨论（BMAD 风格）
- [x] 3.3 编写 Brainstorming 文件产出规范
- [x] 3.4 在阶段四（TDD 实现）入口处添加 Path B 的分支创建逻辑
- [x] 3.5 修改阶段二（分支创建）的标题和前置条件标注

## 4. 更新语言和元信息

- [x] 4.1 将标题从 `# Jira Start` 改为更通用的描述（因为现在支持非 Jira 场景）
- [x] 4.2 更新 `description` 字段，反映新增的开放讨论能力
- [x] 4.3 更新 `argument-hint` 和 `arguments`，确保路由逻辑正确处理
