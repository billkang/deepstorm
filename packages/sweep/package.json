{
  "name": "@deepstorm/sweep",
  "version": "0.10.0",
  "private": true,
  "description": "DeepStorm 测试侧套件 — 测试生成、覆盖率分析、CI 结果解读",
  "license": "MIT",
  "author": "billkang",
  "keywords": [
    "deepstorm",
    "test-generation",
    "coverage-analysis"
  ],
  "scripts": {
    "test:hooks": "for f in hooks/__tests__/*.test.sh; do echo \"--- $f ---\"; bash \"$f\" || exit 1; done",
    "test": "pnpm test:hooks"
  },
  "dependencies": {
    "@inquirer/checkbox": "^4.0.0"
  },
  "files": [
    "skills/",
    "README.md"
  ],
  "publishConfig": {
    "access": "restricted"
  }
}
